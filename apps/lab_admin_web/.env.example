# Optional in local dev: the app defaults to http://localhost:3000 when you run `vite`.
# Required for production builds (`vite build`) so the admin UI knows where to call the API.
#
# VITE_API_BASE_URL=http://localhost:3000
